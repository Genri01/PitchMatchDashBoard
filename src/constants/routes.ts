export enum Routes {
  HOME = "/",
  LOGIN = "/login",
  USER = "/user/:id",
  USERS = "/users",

  FIELD = "/field/:id",
  FIELDS = "/fields",
  CREATE_FIELD = "/field/create",
  EDIT_FIELD = "/field/edit/:id",

  GAME = "/game/:id",
  GAMES = "/games",
  CREATE_GAME = "/game/create",

  GAMES_CALENDAR = "/calendar",
}
